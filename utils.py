from aiogram.utils.helper import Helper, HelperMode, ListItem

class botStates(Helper):
    NOW_STATE = ListItem()
    TODAY_STATE = ListItem()
    TOMORROW_STATE = ListItem()
    FIVE_DAYS_STATE = ListItem()
    SETTINGS_STATE = ListItem()

