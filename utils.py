from aiogram.utils.helper import Helper, HelperMode, ListItem

class botStates(Helper):
    STATE_NOW = ListItem()
    STATE_TODAY = ListItem()
    STATE_TOMORROW = ListItem()
    STATE_FIVE_DAYS = ListItem()
    STATE_SETTINGS = ListItem()

